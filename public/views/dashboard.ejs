<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>User dashboard </title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <!-- Latest compiled and minified CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
    />
    <link
      rel="stylesheet"
      href="https://pro.fontawesome.com/releases/v5.10.0/css/all.css"
    />
    <style>
      .card {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.25);

        margin-top: 50%;
      }
      .user-image {
        width: 150px;
        height: 150px;
        border-radius: 50%;
        object-fit: cover;
        margin-bottom: 20px;
        padding: 10px;
      }
      .user-name {
        font-size: 24px;
        font-weight: bold;
        margin-bottom: 10px;
      }
      .user-email {
        font-size: 18px;
        margin-bottom: 10px;
        color: #999;
      }
      

      .background_pic {
        background-image:url('https://gratisography.com/wp-content/uploads/2023/04/gratisography-neon-lightbulb-free-stock-photo-1170x780.jpg');  
        background-repeat: no-repeat; background-attachment: fixed;
        background-size: cover;
      }

    </style>
  </head>
  <body class="background_pic">
    <section class=" ">
      <div class="container  ">
        <div class="justify-content-between row"> 
          <div class="col"> <h1 class="font-weight-bold text-white"> Dashboard</h1> </div>
          <!-- <div class="col"> <h1 class="font-weight-bold text-white"> <%= user.displayName %> </h1> </div> -->
        </div>
        
        <div class="row">
          <div
            class="col-sm-8 offset-sm-2 col-md-6 offset-md-3 col-lg-4 offset-lg-4"
          >
            <div class="card">
              <img
                class="user-image"
                src="<%= user.photos[0].value %>"
                alt="User Image"
              />
              <h3 class="user-name"><%= user.displayName %></h3>
              <p class="user-email"><%= user.emails[0].value %></p>

              <a href="/logout" class=" mb-3 btn-success text-dark  btn-lg text-decoration-none btn-outline-danger">Logout</a>
            </div>
          </div>
        </div>
      </div>
    </section>
    <!-- Latest compiled JavaScript -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>